services:
  - type: web
    name: agent-task-analyzer
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: MODEL
        value: claude-sonnet-4-6
      - key: NODE_ENV
        value: production
